version: "3.9"
services:
  backend:
    build: .
    image: hookahcalc-backend:dev
    container_name: hookahcalc-backend
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    volumes:
      - hookah-data:/app/data
    restart: unless-stopped

volumes:
  hookah-data:
